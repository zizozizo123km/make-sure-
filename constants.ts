import { StoreProfile, Category, Product } from './types';

// We have removed MOCK_STORES to ensure the app uses the Real Database (Firebase).
// Stores will now only appear if they register in the app or are added to the database.
export const MOCK_STORES: StoreProfile[] = []; 

export const MOCK_PRODUCTS: Product[] = [];